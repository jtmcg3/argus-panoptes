# Argus-Panoptes Default Configuration

[provider]
provider_type = "ollama"
model = "lfm2:24b"
api_url = "http://host.orb.internal:11434"
timeout_ms = 30000

[llm]
provider = "openai"
model = "lfm2:24b"
api_url = "http://host.orb.internal:11434"
max_concurrent_requests = 2
# api_key = "" # not needed for Ollama

[llm.retry]
max_retries = 3
initial_delay_ms = 500
max_delay_ms = 30000
backoff_multiplier = 2.0

[search]
# SearXNG instance for web search (research agent)
url = "http://100.85.147.105:8888"
max_results = 5

[memory]
db_path = "./data/memory"
embedding_model = "all-MiniLM-L6-v2"
max_context_tokens = 4096

[memory.working]
size = 20
